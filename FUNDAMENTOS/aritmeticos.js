// os operadores aritimeticos são binarios, pois operam com 2 operandos

const [a, b, c, d] = [3, 5, 1, 15];

const soma = a + b+ c + d;// sintaxe infix

// postfix

// Ex:  a++;

// prefix

// Ex: ++a;

const subtracao = d - b;

const multiplicacao = a * b;

const divisao = d / a;

const modulo = a % 2;

console.log(soma, subtracao, multiplicacao, divisao, modulo);